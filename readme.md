simple rest full api with fastapi.

To start the server, from the console enter the following command: uvicorn main:app --reload. That allows the uvicorn server to be automatically reloaded.

CRUD: 
    
    -Create
    -Read
    -Upload
    -Delete